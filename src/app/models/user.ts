import { Byte } from '@angular/compiler/src/util';

export interface User {
  userId: number;
  firstName: string;
  lastName: string;
  nationalityId: string;
  birthYear: number;
  email: string;
  photo: string;
  status: string
}
